// Créé un test unitaire qui test la liste d'items.

import { render, screen } from '@testing-library/react';
import ListComponent from './ListComponent';

test('renders list items', () => {
 // *******
});